# Story history

One line per finished story, appended by `/build` when it moves the file here.
Format: `- NNN — <title> · <sprint or —> · <one-sentence result>`.
- 055 — A `drop_` alias is a drop, with two toggles instead of two checkboxes · S11 · a shared `isDropEntry`/`dropStateFor` rule now recognises drops by name+body everywhere, launcher drops render `drop_<slug>`, and the Options cell on both Controls and Aliases shows two accessible icon toggles instead of two checkboxes.

The full texts live next to this file. This index is the fast overview — do not turn it into a
second roadmap.

- 074 — The Library turns nothing into a playable Q2PRO demo installation · S16 · a four-step
  wizard downloads Q2PRO and the free demo data through a verified, loopback-fixture-provable job
  pipeline, assembles a `baseq2`-only installation via an allowlist (never `ctf`/`xatrix`/`rogue`),
  and marks it "Demo" everywhere its engine-computed status is shown.

- 032 — Downloads icon shows a running-count badge · S16 · the titlebar's Downloads button now
  carries a `Badge`-based running-count badge, driven generically per utility module from the
  renderer's own job store (`countActiveJobs` over `moduleId`+`isJobActive`), with a
  pluralised count-aware `aria-label` and a machine-verified e2e flow.

- 001 — Config module scaffold and central profile store · S01 · module wired in with a persisted
  `configProfiles` store and a master/detail create/rename/delete view.
- 002 — Profile-installation assignment and default profile · S01 · many-to-many assignment with a
  pure, unit-tested "one default per installation" invariant and a reconcile sweep.
- 003 — Settings/cvar editor with per-engine defaults and clamps · S01 · ported source-cited
  r1q2/Q2PRO/vanilla cvar facts, engine-scope selector, cross-engine disagreement badges.
- 004 — Write profile to assigned installations on save · S01 · backup-once/diff-skip atomic
  writer with a preview modal; running installations queued as pending.
- 005 — Import an existing config into a new profile · S01 · config tokenizer + `exec`-resolving
  reader feeding a new "import from installation" profile-creation path.
- 006 — Keybinding editor with alternate binding layers · S02 · hold/toggle alt-layer alias
  generator plus per-layer key binding on the existing keyboard overview.
- 007 — In-session profile-switch bind · S02 · n-way self-rewriting alias chain cycling assigned
  profiles in-game, session-only, never touching the stored default.
- 008 — Advanced tab — categories, messages, macros, symbol picker · S02 · action-catalog,
  message composer with meta-variables/server macros, and a byte-faithful Latin-1 symbol picker.
- 009 — Multi-engine validator · S02 · validates the actual rendered profile bytes against every
  assigned engine's real limits, equally weighted, live against unsaved edits.
- 010 — Cleanup of redundant per-mod config copies · S02 · reviewed scan/remove/undo for
  duplicate per-mod `.cfg` files, backed by the shared backup-once contract.
- 011 — Assign an alt layer's trigger by binding it to a key, not at creation · S03 · `triggerKey`
  is now assignable/reassignable/clearable from the keyboard overview, not just at layer creation.
- 013 — Compact the empty alt-layers state and move the layer switcher next to the keyboard
  overview · S03 · one-line empty hint, base/layer switcher relocated into the overview header.
- 014 — Show an alt-layer trigger's action on its own keycap, and switch layers by clicking it ·
  S03 · trigger keys render their target layer and switch it on click in idle mode.
- 012 — Raw config view with reveal-in-folder · S03 · per-installation rendered-file preview with
  an on-disk badge and reveal-in-folder action.
- 015 — Advanced tab — dual-bind editor for Movement, Weapons and Weapon dropping · S03 ·
  catalogue-row Primary/Secondary bind editor with collision Cancel/Replace, replacing the
  free-form action list for these three built-in categories.
- 016 — Auto-create an Alt/Ctrl/Shift layer when a modifier is held during key capture · S03 ·
  modifier binding stored on the action itself, layer overrides a derived mirror of it — the
  redesign that finally closed AC 5 after three prior review-fix cycles blocked on row identity.
- 026 — A committed UI verification harness that drives the real app · S04 · Playwright-driven
  screenshot + axe-core harness (`npm run ui:verify`) with an isolated fixture, 14-screen registry
  and flow API; surfaced a pre-existing `config-raw` rendering bug as a follow-up.
- 017 — Overview — editing is the default, no dedicated edit mode · S04 · removed the edit-mode
  toggle; a keycap click opens `KeyBindDialog` directly outside test mode, trigger-keycap
  click-to-switch retired in favour of story 018's physical-key path.
- 018 — Test mode: trigger keys switch layers, pressed keys light up, readout always visible · S04 ·
  pure resolver + reducer drive the displayed layer from physical trigger presses, every held
  key/mouse button rings on its keycap, readout moved permanently into the legend row.
- 019 — Controls: type the entry, not the category, and let me order entries · S04 · `kind`
  moved onto each entry (bind/message/alias) instead of the category, aliases render as their
  own name and are never bound, entries reorder by hand, and undefined/unreferenced aliases are
  reported in the Validation panel.
- 020 — Controls: rename Advanced and rebuild it as the column-grid prototype · S04 · Advanced
  tab renamed to Controls end to end, rebuilt as a capped, sticky-header column grid with
  always-visible bind slots, a profile-wide conflict count, a filter, a scrollable category
  rail and whole-profile Restore defaults.
- 021 — Settings: rebuild as the dense-rows prototype · S04 · Settings tab rebuilt as a capped,
  dense-rows grid grouped by real cvar group with sticky headers, per-row engine caveats/reset,
  filter/changed-only, whole-profile Reset all, and an Advanced collapse; story 009's
  autosave/facts/honesty rules unchanged.
- 022 — A profile is a real `<name>.cfg` that exists before any assignment · S05 · every profile
  gets a canonical `<name>.cfg` in userData, name-based per-installation copies with collision
  handling, migration of old id-based files, and a live `syncState`/retry contract for stories
  023/025 to render — a review-caught rename-clobber bug was fixed with a cascading re-sync plus
  an independent overwrite guard.
- 023 — Raw File absorbs Write targets — see and open the profile's file anywhere · S05 · Write
  targets tab removed; Raw File now always shows the profile's own canonical file plus one row per
  assigned installation (present/differs/not-on-disk, open in editor, reveal in folder, played
  mods), auto-write moved to a tab-independent hook, and the pre-existing config-raw double-unwrap
  crash was fixed as a bonus.
- 024 — Read the config in the launcher with Quake 2 syntax highlighting · S05 · a pure,
  lossless, positional config tokenizer plus a memoized, gutter'd `ConfigCodeView` with a
  container-scoped find-in-file control now renders Raw File, the write-preview dialog and the
  import preview, replacing `CodeBlock` everywhere; a review-caught CSS grid track-sizing bug that
  broke gutter alignment and a dead `plusCommand` classification were both fixed before landing.
- 025 — Validation becomes Care — report, tidy-up actions and sync state in one place · S05 · the
  Validation tab is now Care: unchanged story-009 report, a story-022 sync section (5 states,
  retry), a new tidy-up section (6 finding kinds, per-item preview/apply, "fix all safe findings"
  batch), preserved lines folded in, and story-010's mod-copy cleanup moved here scoped to the
  profile's installations; a review-caught cross-engine badge-dedup bug, a false "all clear" on
  unvalidated profiles, and a summary that vanished on a sync-fetch error were all fixed before
  landing.
- 031 — Rename Install module to Downloads, move nav entry next to Settings · S06 · full `install`→`downloads` rename (id/route/ipc/i18n) plus a new icon-only titlebar button next to Settings replacing the primary-nav entry; `ui:verify` covers the new screen and a clean review passed on all acceptance criteria.
- 030 — Titlebar and wordmark scale up · S06 · titlebar grows 44px→68px via `--titlebar-h`, wordmark doubled, nav/Settings/Downloads/window controls all resized to the 44px hit-area floor; `ui:verify` confirms the new proportions and a clean review passed on all acceptance criteria.
- 033 — Planned-module screens explain the feature, not the engineering · S06 · Mods/Assets/Downloads planned screens now show plain-language intro + highlights instead of a capability checklist, id/route/ipc debug line is dev-only, `ui:verify` covers all three screens with a clean review pass.
- 029 — Drop-row team message as checkbox + inline row (mirrors "With ammo") · S06 · drop rows (weapon/ammo/misc) now show a "With message" checkbox next to "With ammo" that reveals an inline row with the current text and an Edit button opening the unchanged rich `MessageEditor` (no key capture); a new live-smoke Playwright flow and a clean review confirmed all acceptance criteria.
- 035 — CSP applies in production · S06 · production renderer now served from q2launcher:// with an enforced CSP and a harness regression guard.
- 036 — `handle()` requires a payload schema · S06 · all ~60 `handle()` call sites (shell + module seam) now take a required zod schema, validation centralized in two wrappers (`handle` throws, `handleOutcome` resolves a failed `Outcome`), new IPC + module-registry coverage tests; `ui:verify` and a `story-review-hard` review both passed clean.
- 027 — UI verification runs in one session per fixture, without stealing focus · — (ad-hoc, post-S04) · `ui:verify` went from 56 app starts per full run to 2 (one session per fixture variant, screenshot+axe in the same visit) with `Q2L_UI_HARNESS=1` gating `focusable:false`/`showInactive()` so a run never takes keyboard focus; a review-caught mid-variant crash that stranded the remaining screens and a swallowed liveness failure were fixed, and the experiential focus check was confirmed by the user at S06 acceptance.
- 037 — `ui:verify` covers every surface and its report is green · S06 · write-preview and import-preview dialogs are now screen registry entries (17 screens × 2 viewports), and a full production-mode run went from 4 critical + 16 moderate violations to zero via two structural fixes (a shared `Field`/`useId()` control-id helper, `Modal.tsx` no longer portalling duplicate banner/contentinfo landmarks); `page-has-heading-one` is deliberately disabled and named in the report, and story 027's focus check closed with this one at S06 acceptance.
- 038 — No alias line for an action the engine can bind directly · S07 · the writer now filters an action list through a new shared alias-reference graph before rendering alias lines, dropping a catalogue row's dead alias while keeping every genuinely referenced one, backed by a fixture-corpus invariant test and a live ui:verify fixture; a mid-build follow-up closed a quote-wrapped-reference detection gap and a clean review passed with only non-blocking notes.
- 039 — Aliases get readable names I control, and must be unique · S07 · alias names in the rendered config flip from `q2l_a_<slug>_<id4>` to a short readable slug (with an optional user-chosen override, validated and unique-checked), mirror ownership across all five prefix-dependent sites moved to key-scoped value matching instead of the prefix, legacy profiles migrate their `q2l_a_*` binds/overrides on read, and Care warns (never auto-fixes) on name collisions or a derived name shadowing a reserved command; two review cycles (`story-review-hard`) caught and fixed a byte-compat regression in the legacy-name reproduction, a UI input cap that made the too-long rejection unreachable, and an over-broad Care-warning exclusion that silenced the exact self-referential-alias case (`weapnext`) the story exists to warn about, plus a rename-refusal escape-hatch bug found independently; verified live via `ui:verify` and a new `ui:flow` probe driving the real rename dialog.
- 040 — The profile file is written structured, commented and human-readable · S07 · `renderProfileFile` rewritten from a flat sorted dump into commented, column-aligned sections (header, cvars by group, aliases/binds by category, one section per layer, "other" catch-alls, empty sections omitted) with a trailing `// <label>` on every generated bind/alias, plus a new per-profile `writeUnbindall` setting and its Raw File tab checkbox; `story-review-hard` caught and fixed a raw NUL byte hiding in a reverse-index separator (which had also made the file binary to grep), a case-insensitive-duplicate determinism gap in cvar ordering, and an unbounded banner line-length edge case, before passing; verified live via `ui:verify` (0 axe violations, screenshots of the new layout in the Raw File tab and write-preview dialog) on top of 1228 green tests.
- 041 — Import understands aliases, press/release pairs and unbindall · S07 · import now parses `alias` (plain, press/release, empty-body, self-rewriting, `bind`-in-body) into real Controls-tab entries instead of preserved text, resolves alias references across `exec` chains and duplicate names, and recognises colour cvars in the message editor; a `story-review-hard` pass ran the parser against adversarial input and the real `docs/fixtures` player configs and caught 3 real bugs (empty-body aliases vanishing on write-back, colour-cvar bytes at the real 0x7F boundary going unrecognised, a paired alias's release half staying permanently `aliasUnreferenced`), all fixed and re-verified; live `ui:verify` run (18/18 screens, 0 axe violations) confirms the new import-review step.
- 046 — The production CSP no longer allows inline styles · S08 · `style-src` in `PRODUCTION_CSP` dropped `'unsafe-inline'` (`DEV_CSP` untouched for Fast Refresh), backed by a unit-test regression guard and a live `ui:verify` guard requiring `style-src 'self';` in the served header plus a `securitypolicyviolation` collector wired all the way into the exit code; a review-fix cycle closed a gap where violations were collected but not actually gating the run; verified live with zero visual regression against a pre-change baseline, zero a11y findings, and zero CSP violations, and `docs/ARCHITECTURE.md` now names the permitted (React `style` prop / CSS custom properties) vs. forbidden (`setAttribute('style', …)`, literal `<style>`, `dangerouslySetInnerHTML`) forms.
- 047 — The message editor is covered by ui:verify like every other surface · S08 · MessageEditor, RemoveInstallationDialog and DetectDialog joined the screen registry (22 screens total); ui:verify full run is green with 0 axe violations at every impact level.
- 042 — A launcher-written profile file re-imports without losing anything · S08 · a versioned `[q2l ...]` trailing-comment tag on each bind/alias line plus fixed-anchor category section headers (up to two levels, style-configurable) carry display name, kind, catalogue id, slot pairing/modifier, category and layer membership through render→parse→restore as a fixed point, with the profile id never adopted from an imported file and the import dialog distinguishing "restore" from "best-effort"; eight adversarial review rounds against constructed edge-case profiles (not diff reads) found and fixed real defects in slot-conflict handling, banner-title decoration stripping, untagged-alias/category recovery and a switch-bind/layer-alias false-positive, leaving three narrow hand-edit-only limitations documented rather than silently left; verified live via `ui:verify` (0 axe violations, 46/46 screens) on top of 1477 green tests.
- 043 — The .cfg file is the source of truth, state.json becomes a cache · S08 · inverts story 022 decision 8 for the canonical file only: explicit Save + unsaved-changes indicator replace write-on-every-keystroke, a re-read-before-write gate keyed on the file's real on-disk bytes (not just a dirty flag) refuses to clobber an unread hand-edit, external edits are adopted silently or surfaced as a real two-pane conflict dialog depending on whether the profile is dirty, a deleted/corrupt `state.json` record rebuilds from its file with the same id, and existing profiles migrate to the current file format once on first run; an adversarial cross-cutting pass (not a diff read) constructed and ran external-edit, racing-write, corrupt-file, deleted-file, migration and rebuild scenarios against the real pipeline and found/fixed three real defects beyond what the per-deliverable tests caught, leaving two narrow limitations documented rather than silently absorbed; verified live via `ui:verify` (0 axe violations, 48/48 screenshots, 24/24 screens) on top of 1612 green tests.
- 048 — Every setting is written to the file, and nothing resets to default any more · S09 · render.ts now writes a `set` line for all ~30 catalogue cvars on every render, read-back strips them back out via a new shared `cvar-defaults.ts` helper so state never inflates and story 042's round-trip fixed point still holds, all three reset/restore-to-default affordances (Settings per-row, Settings "Reset all", Controls "Restore defaults") and dead `suggestedKeys` are removed, and the Settings "changed" filter/counters/row indicator switched to an "edited and unsaved" baseline; a story-review-hard pass found and fixed a real data-loss regression in the new toggle/text-kind default comparison before passing on re-review; verified live via `ui:verify` (0 axe violations) on top of 1671 green tests.
- 049 — I can see what an unsaved change is, review it, and throw it away · S09 · a `ProfileBaseline` snapshot seeded everywhere `fileHash` already is, plus a pure `diffProfileAgainstBaseline` over cvars/binds/actions/layers/settings, backs a new `discard` IPC handler and a `ProfileChangesContext` feeding the save bar's expandable before/after view + Discard button and non-colour-only row markers on Settings/Controls/Layers/Raw File, replacing story 048's renderer-local dirty tracking; a story-review-hard pass found and fixed a rename-survives-discard gap and an order-dependent live-smoke flake; the P2 live-smoke gate was blocked in the build agent's environment by a misdiagnosed EPERM, root-caused by the orchestrator to Windows holding an Electron session's closed GPU shader-cache directory for up to ~48s with no live process attached, fixed at the source with a sized `rmSync` retry budget in `scripts/lib/fixture.mjs`, which also surfaced and fixed a `screens.mjs` strict-mode locator collision from this story's own new "unsaved changes" text; verified live via `ui:verify` (0 axe violations, 52/52 shots, 26/26 screens) on top of 1731 green tests.
- 044 — One surface to manage every alias in a profile · S09 · a new Aliases tab lists every user, generated and layer alias from one shared `buildAliasIndex` reference graph that `validate-actions.ts`/Care now consume too, with sort/filter, explicit empty/duplicate states, full CRUD (create/edit via `ActionEditor`, rename via an extracted `RenameActionDialog` keeping story 039's refusal rule, referrer-naming delete confirmation) and two-way deep links with Care and Controls/Overview; a story-review-hard pass found and fixed a dead-click layer owner link, a missing over-budget indicator for a single too-long command line, and a silent Care deep-link edge case; verified live via `ui:verify` (0 axe violations, 54/54 shots, 27/27 screens) on top of 1764 green tests.
- 052 — Controls shows my categories, the template only seeds them · S11 · categories become ordinary, persisted, ordered `profile.categories` data (no more built-in Movement/Weapons/Weapon-dropping special case) across the shared render/restore/import/labels/tidy-up core and the renderer rail/rows, unbound entries round-trip through a new commented bind line, a one-time migration materialises every catalogue row into pre-existing profiles so nothing disappears, and the rail/rows gain uniform rename/delete/move plus catalogue/template suggestions and a delete-or-move category dialog; a story-review-hard pass (FAIL, 10 findings) found and fixed a migration bug that silently un-bound already-bound keys, a category-order-lost-on-restore edge case, two `ui:flow` regressions and three smaller issues, leaving one narrow limitation (an empty category doesn't survive rebuild-from-file) documented rather than silently absorbed; verified live via `ui:verify` (0 axe violations, 29/29 screens) on top of 2131 green tests.
- 053 — Sub-categories come from the file, and Controls shows them · S11 · `ConfigActionCategory.subcategories`/`ConfigAction.subcategoryId` make a real, profile-owned second level (exactly two deep) that the writer/reader round-trip losslessly via a tagged `sub=<id>` banner nested in its category's own section, including an empty sub-category surviving reload; the Controls grid derives group headers from the profile instead of a catalogue-id-prefix map, with create/rename/reorder/delete on the header and a sub-category select in the action editor, all riding the existing `patch({ categories, actions })` channel; a foreign-file importer heuristic turns a repeated-decoration main+sub header pair into a real category+sub-category instead of the old `Main / Sub` name-flattening; a story-review-hard pass (FAIL, 6 findings) found and fixed a blocking gap where a `.: Title :.`-style outer header opened no section at all (predating this story, traced to a pre-existing hole in 042's own removed `pairedTitle`), leaving 5 non-blocking findings documented as residuals (empty-sub-banner file noise across all block types, duplicate-id/hand-edited-name edge cases, missing direct delete/move unit test); verified live via `ui:verify` (0 axe violations, 30/30 screens) and `ui:flow -- controls-subcategory` on top of 2166 green tests.
- 056 — Extra keys group under the primary row · S11 · one Key column replaces the Primary/Secondary pair, with a numeric-slot write layer (a clear now compacts `action.keys` instead of story 050's in-place blank), extra keys reusing `BindSlot` unchanged as indented sub-rows that fold behind a "+n" chevron from two extras, and promotion/counts/Options scanning every slot in order; a three-cycle story-review-hard pass found and fixed a plain-row raw/compacted index bug, a duplicated primary-slot render, and a collision self-exclusion regression introduced by the first fix, before passing with the residuals (ActionEditor's raw-slot read, a 10px narrower Options track, one dead helper) documented; verified live via `ui:verify` (0 axe violations, 64/64 shots) and `ui:flow -- controls-extra-keys` on top of 2179 green tests.
- 051 — The file header is a small banner, not a technical block · S12 · profile `.cfg` files now render a 4-line cosmetic banner (`=` rule / name / `=` rule / right-aligned `[q2l v=1 id=<uuid>]` tag) instead of the old 5-line sentinel+banner block, with ownership carried as a new `id` key in the existing tag grammar and a new shared `file-ownership.ts` module as the single place every main-process reader and the restore reader check it against (full file content, not line 1); the old sentinel shape is still read and migrates to the new shape on next save, the loader is untouched, and a real blank-name trailing-whitespace bug plus several stale sentinel-only assertions surfaced by the round-trip/adversarial pass were fixed; a story-review-hard pass (PASS) found only low-severity stale doc comments, fixed in a follow-up; verified live via `ui:verify` (0 axe violations) showing the real running app render the new header with syntax highlighting intact, on top of 2276 green tests.
- 059 — Settings mirrors the file's own sections · S12 · `profile.cvarSections`/`writeCatalogDefaults` make Settings mirror the file's own two-level cvar sections (052/053's model one level down), with a `cvs=`/`cvsub=` tagged writer/reader round-trip, an importer that files each cvar under its first-seen banner while folding to the last-set value, a one-time migration that seeds the full four-group catalogue for pre-existing profiles, a renderer that groups rows by section with a plain row for every non-catalogue cvar, full section/sub-section CRUD plus add/remove/move-cvar, and a Settings-header toggle for whether unplaced catalogue defaults get written at all; two story-review-hard rounds found and fixed four blocking bugs (a cvar-name-list cap that bricked Settings saves on real-world imports, last-value-wins import attribution that broke the literal 25-cvar dm.cfg acceptance criterion, an unimplemented sub-banner heuristic, no way to move a cvar out of the reserved Defaults bucket) plus three should-fix items, leaving four low-severity residuals documented rather than silently absorbed; verified live via `ui:verify` (0 axe violations, 64/64 shots, 32/32 screens) and `ui:flow -- settings-section-rename-add-cvar` on top of 2374 green tests.
- 057 — Raw file is an editor first, and I can edit inline · S12 · `ConfigCodeView` gains an `editable`/`fill` mode (transparent-textarea-over-tokenised-`<pre>` overlay, no runtime `<style>`, CSP-safe) with a pure indent helper and Ctrl+F find; the raw tab fills the viewport (≥30 lines at 1280x800), compacts to one path line and one file-options toolbar row, and drops the per-installation cards (moved to story 058); a new `config:saveRawText` main handler writes exactly the typed bytes under the existing conflict guard and adopts the result back, backed by a renderer `raw-draft` context that makes typing an unsaved change with its own save/discard/conflict-overwrite path, mutual exclusion against structured changes in both directions, and an inline read-back panel; three story-review-hard rounds found and fixed a short line-budget, a mutual-exclusion bypass via three ungated controls, and a focus-resume conflict-guard bypass, plus fixed an unrelated dev-environment jsdom/Node incompatibility that had been silently skipping a hard-tier deliverable's only test file; verified live via `ui:verify` (0 axe violations, 64/64 shots) and `ui:flow -- raw-inline-edit` on top of 2458 green tests.
- 054 — Order everything by drag and drop · S13 · every ordering surface in Config (Controls rows within/across sub-categories and categories, sub-category headers, category chips, Settings sections/sub-sections/cvars) is now reorderable by real drag-and-drop via `@dnd-kit`, sharing one `DndContext` per surface, with a translated keyboard sensor/announcements path kept alongside (a new Controls row kebab menu with Move up/down/Move to…, existing Settings arrow buttons/`MoveCvarDialog`); pure array-position helpers back every reorder and all persist through the existing save/dirty/Discard path, with a genuine pre-existing gap (`cvarSections` never captured/restored by baseline/Discard) found and fixed along the way; mid-build an agent accidentally reset `ControlsTab.tsx` to its pre-story HEAD version, destroying several deliverables' wiring in that one file, which a follow-up agent faithfully reconstructed from the surviving supporting files and tests; a story-review-hard pass (PASS, 6 findings — an untranslated dnd-kit instructions string, row announcements speaking raw ids, a required-schema-field baseline-breaking bug, an overly global Settings drag-disable check, a rendered/real index mismatch next to unrendered cvar names, and a broken sub-row indent) were all fixed in one review-fix cycle; verified live via `ui:verify` (0 axe violations, 64/64 shots) and `ui:flow -- controls-drag-reorder` on top of 2541 green tests.
- 058 — Care says only what needs doing · S13 · Care becomes one derived to-do list: `lib/care-items.ts` folds validation findings, non-`inSync` sync rows and tidy-up findings into a grouped, errors-first `CareItem[]`, and `lib/care-summary.ts`'s `allClear` now requires zero items *and* every source resolved, keeping "nothing to validate against" an explicit third state; `CareTab.tsx` renders either one All-clear block or the groups that have items via a shared `CareItemRow`, deleting `ValidationPanel.tsx`/`PreservedLinesPanel.tsx`/`CareSyncSection.tsx`/`CareTidyUpSection.tsx`/`RawConfigPanel.tsx`; the redundant-copies cleanup moves out of Care entirely onto a new per-installation Library dialog, and a "Show in Controls" deep link joins the existing "Show in Aliases"; two story-review-hard cycles found and fixed a blank-tab-on-sync-error regression (AC 2), a nested-modal-Escape and a follow-on keyboard-Tab-trap route that could both strand the destructive cleanup flow's "Undo removal" affordance, dropped fix-hint/source text and file-failure messages, and doc/dead-code drift, before passing with residuals none; verified live via `ui:verify` (0 axe violations, 34 screens/68 shots) and `ui:flow -- care-fix-item` on top of 2565 green tests.
- 060 — Duplicate alias is fixable from Aliases · — · Care's "Show in Aliases" deep-link on an `aliasDuplicate` finding now carries the finding's `ownerActionId` through to `AliasesTab`'s focus effect, so it highlights the exact colliding row instead of the first row matching the shared name (non-duplicate deep-links keep their name-only fallback unchanged); the "DUPLICATE" badge and Care's automatic re-check-on-save were already correct and needed no change; code review (default tier) PASS, no findings; verified via `ui:verify` (0 axe violations, 68/68 shots) as a no-regression smoke check on top of 2565 green tests (2 pre-existing `validate-actions.test.ts` assertions updated to include the finding's new `actionId` param).
- 063 — Hand grenades and Grenade Launcher can be bound to a key · S14 · a keyless `kind: 'bind'`/`'message'` entry with a body now gets both its alias line and a `//bind "<aliasName>"` unbound line on write (`render.ts#isUnboundEntry`), and the reader (`profile-restore.ts#inferKind` + entry grouping) reads that signal back as `kind: 'bind'` instead of misreading it as `kind: 'alias'`, which is what left the Hand grenades/Grenade Launcher rows permanently inert; an adversarial round-trip pass covers the fix plus AC4's four-distinct-`catalogId`s case, and a Controls row menu "Make bindable" repair action heals already-damaged profiles in place, pinning the alias name so a `+signed` one survives the kind flip; a story-review-hard pass (PASS) found two stale doc paragraphs in `docs/systems/profile-file-format.md` and a stale Acceptance Tests mapping in the story itself, all corrected, plus two low-severity, non-blocking trade-offs documented rather than fixed; verified live via `ui:verify` (0 axe violations, 68/68 shots) and `ui:flow -- grenade-rows-take-a-key` on top of 2607 green tests.
- 061 — Profile header is one row: back left, identity centred, actions right · S14 · the profile detail header collapses to one `<header data-testid="config-profile-header">` shared by all seven config tabs (back left / `config-profile-identity` centred with name+created+updated+unsaved / `config-profile-actions` right), the Raw File tab's own folded two-row header is gone, and a new `scripts/flows/config-header-geometry.mjs` flow measures rather than assumes the 30-line Raw File editor floor (final: 31 lines / 18px margin at 1280x800), funded by merging the Raw tab's two toolbar rows into one and tightening `.cfg-code--fill` padding; a clean-agent review (PASS) found two test-rigor gaps (AC1's single-row bound, AC2's no-duplicate-identity-block check) rather than implementation defects, both closed by strengthening the flow; verified live via `ui:verify` (0 axe violations, 68/68 shots) and `ui:flow -- raw-inline-edit`/`config-header-geometry` on top of 2607 green tests.
- 062 — Controls category rail is a clean chip row with an action menu · S14 · the category rail chip collapses to one visual level (`.ctrl-category-chip` owns the sole border/background, `data-selected`/`data-category-id`/`data-category-name`, a ghost label button with `aria-pressed`, grip + one kebab that reveal on hover/focus/drag/selection/open-menu), replacing the permanent four-icon-button toolbar with a new `ControlsCategoryMenu.tsx` (mirroring `ControlsRowMenu.tsx`) holding Move up/Move down/Rename…/Delete… behind one trigger; the chip's three simultaneous roles (sortable drag item, story 054 D5 row-drop target, scroll-into-view ref) stayed on one ref-composed node, guarded by unmodified, still-green `ControlsTab.category-drag.test.tsx`/`ControlsTab.dnd.test.tsx`; a default-tier clean-agent review (PASS, 0 findings) also verified the `.ctrl-chip-drop-over` vs `[data-selected]` CSS-specificity fix resolves correctly; verified live via `ui:verify` (0 axe violations, 68/68 shots) and `ui:flow -- controls-category-rename-reorder` on top of 2614/2615 green tests (1 unrelated pre-existing Windows file-lock flake in `file-source-pipeline.test.ts`, 85/85 in isolation).
- 064 — Unsaved changes read as a real diff · S14 · `profile-diff.ts` adds a `ProfileChangeDetail[]` `details` array per changed entry (derived structurally from the union of both rows' top-level keys, not a hand-written field list), dropping the old canonical-JSON fallback so no section can render an unlabelled blob; `ProfileChangeList.tsx` renders a text `added`/`removed`/`changed` badge plus a summary before→after and a nested per-field detail list in a bounded/scrollable value block, with `settings` labels now translated instead of printing raw field keys; a new `scripts/flows/unsaved-diff.mjs` proves it live by dirtying settings/cvars/actions/binds and asserting label+both-sides on every row, added/removed marked in text, badge-count-equals-row-count, and no value-block overflow; default-tier clean-agent review (PASS, no findings) independently re-verified AC4's count/keys invariant under a 5-action-field + 3-layer-field simultaneous change; verified live via `ui:verify` (0 axe violations, 68/68 shots) and `ui:flow -- unsaved-diff` on top of 2625 green tests.
- 065 — Installation name carries an engine badge everywhere it is shown · S14 · a new `EngineBadge` component (`src/renderer/src/components/ui/EngineBadge.tsx`) holds the `r1q2`→flame / else→neutral tone expression and `engineLabel(kind)` text in one place, replacing the repeated inline expression at four existing badge sites (rail, hero, `AddExistingDialog`, `DetectDialog`), added to the config module's two installation lists (`InstallationProfilesPanel`, `ProfileAssignmentsPanel`) with the name kept shrinkable so the badge survives truncation, and promoting the plain-text engine meta on `LibraryView`/`ActionBar` to a badge next to the name; the UI fixture gained a third populated installation (`engineKind: 'unknown'`, a 156-character name) and a new `scripts/flows/engine-badge-surfaces.mjs` proves all six surfaces live; a default-tier clean-agent review (PASS, one stale-comment finding, fixed) also confirmed no file this story touches overlaps stories 061-064; verified live via `ui:verify` (0 axe violations, 34/34 screens) and `ui:flow engine-badge-surfaces` on top of 2632/2633 green tests (1 unrelated pre-existing timing flake in `import-reader.test.ts`, 32/32 in isolation).
- 068 — The app says engine, not client · S15 · `en.json` now has zero "client" occurrences (executable rows relabelled "Engine executable"), `EngineDefinition` carries a required `supported: boolean` (true only for `r1q2`/`q2pro`) with `SUPPORTED_ENGINE_DEFINITIONS`/`isEngineSupported` as the single source, the create-installation dialog's dropdown is derived from that flag (R1Q2/Q2PRO only, under an "Engine" label), and a new `engineDisplayLabel` helper marks any installation on an unsupported engine as "<label> (unsupported)" as text inside its existing badge/stat everywhere it is shown, while detection keeps classifying and labelling all eight known engine kinds; a default-tier clean-agent review (PASS, no findings); verified live via `ui:verify` (34/34 screens, 0 axe violations) and `ui:flow engine-not-client`/`engine-badge-surfaces` on top of 2648/2649 green tests (1 unrelated pre-existing timing flake in `import-reader.test.ts`, passed in isolation).
- 067 — An installation carries an icon I choose · S15 · a build-time-generated shipped icon set (`scripts/generate-installation-icons.mjs` → 128px `.avif`, discovered via `import.meta.glob`, no component names a file), a shared `InstallationTile` across rail/library-card/action-bar, `InstallationIcon` on `Installation` plus three contract-first IPC channels (`setIcon`/`pickIconFile`/`iconDataUrl`), a main-owned `InstallationIconsService` that validates (extension → size → decode order), re-encodes to a fixed 128px PNG and stores it at `userData/installation-icons/<id>.png` (never a renderer-supplied path), and a `SetInstallationIconDialog` reachable from the library card; records a CLAUDE.md deviation scoping "no image assets in the UI" to the tile and picker dialog only; a two-pass `story-review-hard` review (pass 1: FAIL — a stale `iconDataUrls` cache that kept showing a re-picked/cleared icon's previous `data:` URL until restart, and a rail layout regression from D2's tile extraction that shifted every tile below an icon-bearing one by ~5px) found and fixed both, plus four lower-severity findings (deviation wording, a stale AC9 test-doc mapping after an unworkable e2e IPC-spy approach was replaced with the existing unit-test proof, a fully-transparent fixture icon that made screenshots misleading, a latent accessibility regression where an icon collapsed the library card's accessible name to a generic non-distinguishing fallback); two independent confirmation passes both returned PASS, with one residual (a missing regression test for the cache fix) added afterward and verified to actually catch the bug; verified live via `ui:verify` (34/34 screens, 0 axe violations) and `ui:flow installation-icon-tile`/`installation-icon-pick` on top of 2712/2712 green tests (1 unrelated pre-existing timing flake in `import-reader.test.ts`, passed in isolation).
- 069 — The profile header breathes in two lines · S15 · the config profile header's identity zone became two rows inside the same single `config-profile-identity` element (line 1: name + unsaved indicator, line 2: Created/Updated, smaller and dimmer), funded by a hardening pass (Option B) that made the read-only code view's find bar open on demand (Ctrl+F) instead of always-visible and folded the raw tab's locked hint into the merged toolbar row as a warning badge, so the 30-visible-editor-line floor (`scripts/flows/config-header-geometry.mjs`) holds at `lines=30 margin=10px` in both the `editable` and `lockedByChanges` raw-tab shapes; a three-pass `story-review-hard` review (pass 1: FAIL — the new find bar didn't reliably focus and lost keyboard reachability after Escape; pass 2, after a fix: FAIL — the fix left Ctrl+F a no-op whenever the bar was already open, destructive in the editable branch since keystrokes then leaked into the config text; pass 3, after a second fix: PASS) closed both regressions and added the first e2e find-bar coverage for the editable branch (`scripts/flows/raw-inline-edit.mjs`); verified live via `ui:verify` (68/68 shots, 0 axe violations) and `ui:flow config-header-geometry`/`raw-inline-edit`/`unsaved-diff` on top of 2712/2712 green tests (1 unrelated pre-existing timing flake in `import-reader.test.ts`, passed in isolation).
- 066 — A new profile starts empty, from a handed template, or from my own config files · S15 · the create dialog's "Start from" gained a fourth-option contract (`ConfigProfileSeed = 'empty' | 'template-right' | 'template-left'`, `ConfigProfile.seedFrom`, both templates seeding identically from `STANDARD_TEMPLATE` for now) and a full renderer-driven multi-file import replacing the old `{ installationId, gameDir }` addressing: `readImportableFiles(paths)` shares the reader's fold/exec core with an installation-mode vs. file-mode exec-resolution strategy (file mode confined to the containing file's own folder, no fallback), a new `DialogService` (env-double-gated `Q2L_UI_HARNESS=1` + `isDev` harness stub, unreachable otherwise) hands main-owned absolute paths to a session-scoped `PickedFilesRegistry`, and the renderer only ever holds opaque `{id, fileName, dirName}` handles and sends `fileIds` back, with commit re-reading from disk independently of preview; a single `story-review-hard` pass (PASS, 4 findings) independently traced and confirmed sound both the path-trust boundary and the harness stub's production-unreachability, found a stale "Import from installation" label its own D6 test had pinned (fixed) and a dead i18n string (fixed), and left two non-blocking gaps documented (a retired e2e screen's restore-banner coverage now unit-only; a 64-file import cap with a generic error message); verified live via `ui:verify` (32/32 screens, 0 axe violations) and `ui:flow -- import-from-files` (both the populated and installation-less fixture variants) on top of 2755/2755 green tests.
- 070 — The launcher reads a curated manifest instead of hardcoded download URLs · S16 · a main-process-only manifest pipeline (shared types, zod-validated envelope-refuses/package-drops parser, a raw.githubusercontent transport helper, a `ManifestService` that merges engines/gamedata files, caches via `JsonStore`, and falls back to the cache with a real age on any fetch or refusal failure while refusing to fabricate an empty snapshot on a cold cache, and the `downloads` module main half exposing `manifest.get`) plus the real Q2PRO nightly and both free game-data packages with live-verified SHA256 hashes; a `story-review-hard` review (PASS after one fix cycle) found and fixed two real correctness bugs — `pinnedEnginePackage()` trusting an id match without checking the resolved package's kind/engine, and an unvalidated `engine as EngineKind` cast — plus a comment inaccuracy, leaving a few coverage-gap notes documented as non-blocking; no `ui:verify` surface (main-process only, per the story's own Acceptance Tests section); verified via `npm run build`/`typecheck` (clean) and `npm test` (2792/2792, one pre-existing unrelated timing flake in `import-reader.test.ts` passing in isolation); publishing the manifests to the public content repo and uploading the mirrored Q2PRO release binary is a documented manual residue (no push/publish credentials in this run).
- 071 — A download is a verified job, never a trusted file · S16 · the `downloads` module's main-process download pipeline (own minimal `PackageSource` contract, `net.fetch`-based streamed/hashed fetch with headers+stall timeout and 3-retry-then-mirror-advance semantics, size+SHA256 verification with delete-on-mismatch and no override, a vendored-`7za.exe` extractor invoked with a fixed absolute path and fixed argv/`shell: false`, and a module-owned queue/pipeline producing one `Job` per download through the shell's existing untouched `JobsService` with real cancel cleanup of both the HTTP stream and the 7za child process) plus a layering/CSP regression guard; coexists with story 070's manifest types without importing them, per this story's own refine decision; no renderer half or IPC channel by design ([[074]] wires it later); a `story-review-hard` review (FAIL after pass 1 — AC5's named test asserted nothing about the real spawn() argv, and a confirmed bug where the dev-mode 7za path resolver counted fixed `..` levels that do not match the real bundled main process) → one fix cycle (walk-up-to-`package.json` path resolution, a real spawn-argv assertion, a de-tautologized progress-parsing test, `fetch:7za` wired before packaging, an out-of-scope i18n key removed) → PASS on re-review; verified via `npm run build`/`typecheck` (clean), `npm test` (2848/2851, 3 intentional `skipIf` real-7za-binary skips — the binary was never vendored end-to-end in this sandbox, no network access to 7-zip.org), and `npm run ui:verify` (64/64 screens, 0 axe violations, pure no-regression check since this story adds no renderer surface); AC6's real-surface cancel trigger and the 7za binary's own network-fetch integrity check are documented open points, not blockers.
- 072 — Settings learn to host a module's own section, starting with Downloads · S16 · the Settings
  view now renders module-contributed sections (`RendererModule.settingsSection`, shell-owned
  `Panel`/`SectionLabel` chrome, module supplies only inner content), placed between Library and
  About, sorted by `order`/id, with `View` now optional so a module can appear here without a route;
  the downloads module is the first consumer, extending story 071's already-shipped `downloads`
  state key/1–6 concurrency range (not the plan's stale 1–4/`downloadsSettings` draft) with an
  archive-cache budget and a download-while-playing toggle, a pure `planEviction` core (oldest-mtime
  first, `.part` and in-use files never evicted) plus `status`/`enforceBudget`/`clear`, and a clear-
  cache confirm dialog naming size/count before deletion; a `story-review-hard` review (PASS after
  one fix cycle) found and fixed a confirmed bug where eviction/status logic also matched 070's
  shared `manifest-cache.json` as an evictable archive, plus two confirmed UI-staleness bugs (cache
  size not refreshed after a budget-lowering patch; the clear confirmation hardcoding a `{0,0}`
  result instead of what was actually deleted) and a non-re-runnable e2e flow, leaving a few
  lower-severity PLAUSIBLE findings documented as non-blocking follow-ups; verified via
  `npm run build`/`typecheck` (clean), `npm test` (176/179 story-scoped, 3 pre-existing 7za-gated
  skips; one unrelated pre-existing timing flake in `import-reader.test.ts` under full-suite load,
  passing standalone), and `npm run ui:verify` + `npm run ui:flow -- settings-downloads-section`
  (both clean, the flow re-runnable without a reseed).
- 073 — The Downloads tab shows what is running, what failed, and what is cached · S16 · the `downloads` module got its real `DownloadsView` (job list with progress/cancel, a cache-size `KeyValue`, and a persistent i18n'd failure log with a collapsed "dismissed"-and-restore disclosure) replacing the `PlannedModuleView` placeholder, backed by a new additive multi-listener `JobsService.onChange` observing every `downloads` job into a capped, 7-day-retention `downloadFailures` slice in `state.json`, and a dev-only `dev:simulateJob` scenario trigger (`success | stall | failure`) for offline verification; a `story-review-hard` review (FAIL after pass 1 — a confirmed bug where the succeeded-job fade effect could get stuck forever if the jobs list changed again mid-fade, reachable whenever `concurrentJobs` > 1) → one fix cycle (ref-based one-shot fade timer, the fade test strengthened to actually exercise a mid-fade store update, an unexplained eslint-disable removed, the Acceptance Tests section's test-title cross-references corrected) → verified clean; verified via `npm run build`/`typecheck` (clean), `npm test` (2952/2955, 3 pre-existing unrelated skips), and `npm run ui:verify` (66/66 screens, 0 axe violations) plus `npm run ui:flow -- downloads-tab` (all steps green, offline).
- 075 — A failed download tells me enough to report it · S16 · bootstrap failures now carry structured diagnostics (per-package URL/size/verify/extract, target verdict, missing checks, bounded log tail) with a copy-to-clipboard Markdown report and log-reveal action, home directory redacted at capture; a `story-review-hard` review found six real gaps (missing AC5 test, two AC4 tests that could not fail, an under-matching redaction boundary regex, wrong-end package trimming, a silently swallowed copy failure) → one fix cycle → PASS; verified via `npm run build`/`typecheck` (clean), `npm test` (3098/3098, one pre-existing unrelated timing flake passing standalone), `npm run ui:verify -- --screens=downloads` (axe-clean) and `npm run ui:flow -- downloads-tab` (offline, green).
- 076 — The bootstrap assembles the real archives, not the fixtures · S17 · `assemble.ts`'s allowlist was rewritten to match the three really-pinned archives (candidate `from` paths, engine target read from `ENGINE_DEFINITIONS`, `players`/`video` sourced from `baseq2/`), the job now fails naming the specific incomplete package (`downloads.error.packageIncomplete`), the fixture archives and both e2e flows were re-laid-out to match, and a checked-in `docs/fixtures/archive-layouts.json` + `archive-layouts.test.ts` now keep the allowlist, the fixture and the shipped manifests honest against each other and against reality; a `story-review-hard` review (PASS) found and fixed two coverage gaps (`GLOB_DIRS` never cross-checked against the listing; the fixture-mirrors-listing check only ran one direction) plus completed an interrupted prior review-fix (a stale `PRUNABLE_TARGET_DIRS` root-level `players` path whose fix was already applied but whose regression test was unfinished, left an unused-helper `tsc` error).
- 077 — A failed install stays in my library and shows its last error · S17 · a bootstrap failure now records a persisted `lastFailure` (errorKey/at/jobId, optional interpolation params) on the installation instead of deleting it, clearing automatically once a later verdict is playable and surviving an app restart; the job's failure exit deletes only the assembled files (registration and target root stay), a retry on the same folder adopts the failed installation instead of tripping `installations.error.duplicate`, and cancelling an adopted retry restores its pre-retry registration and failure record rather than deleting it; the Library card and rail tile gained a `FailureBadge`/microtag naming the failure via i18n; resumed after an interrupted review-fix cycle, a fresh `story-review-hard` pass confirmed the three prior findings (raw `{{packageId}}` placeholder, cancel-of-adopted-retry dead end, engine-preservation guard) were fixed but caught two further gaps (an AC8 regression where the engine-preservation guard touched ordinary, never-failed installations too, and an AC2 story-documentation gap for the adopted-retry cancel semantics) plus a stale test left over from the interrupted cycle, all fixed; verified via `npm run build`/`typecheck` (clean), `npm test` (154 files / 3142 tests), `npm run ui:verify` (34/34 screens, 0 axe violations) and `npm run ui:flow -- bootstrap-failure-retry` (offline, green).
- 078 — The failure says the cause, not "go read the log" · S17 · `DownloadDiagnostics` gained two redacted-at-capture, cap-trimmed records (`assembly`: what every allowlist candidate looked for and who served it; per-package `contents`: a bounded top-level extraction listing) plus a derived `contributed` flag, and a new shared `FailureCauseDetail` component (closed-by-default `<details>`, per-package step reached + target verdict/failing checks) is mounted by both the Downloads tab's failure card (reveal-log demoted into its footer) and the bootstrap wizard's failed running step, so a user reads the real cause without opening `main.log`; the copied report gained an assembly table and extraction listing for maintainers; resumed after an interrupted review pass (no findings yet logged), a fresh `story-review-hard` pass independently re-verified the hard-tier redaction/cap-order focus area as correct and returned PASS with 4 medium/9 low findings, two confirmed and fixed (a `contributed: undefined`-vs-`false` ambiguity that told a package "did not contribute" when assembly had never actually run for it; a not-found allowlist entry with multiple candidates recording only the first, hiding that the real-world wrapper-nested layout was tried and still missed) and the rest documented with reasons (a pre-existing, [[075]]-era gap where a parameterized `missingChecks` key renders an unfilled `{{path}}`; the new e2e flow structurally cannot reach a real `installationNotPlayable` verdict, disclosed at length in its own header rather than concealed); verified via `npm run build`/`typecheck` (clean), `npm test` (157 files / 3173 tests, one unrelated pre-existing timing flake passing standalone), `npm run ui:verify` (34/34 screens, 0 axe violations) and `npm run ui:flow -- downloads-tab`/`bootstrap-failure` (both offline, green).
- 079 — A save reaches every installation, and Care sees drift · S17 · every path that changes a profile's canonical file (raw save, adopting an external edit, structured save) now cascades byte-identically to its assigned installations, never a re-render, made trustworthy by render-stable adopt (grouping ids adopted, not re-minted); a running game no longer defers writes (`pending` removed end to end); Care now surfaces drift outside the tab (tab-strip badge, background fetch on focus/profile-open) and offers a one-click *Sync now* per drifted row with wording that tells a hand-edited copy apart from a failed launcher write; a `story-review-hard` review (PASS) found a plausible correctness regression (a clean-but-not-render-fixed-point profile's canonical file could strand at a stale path after a rename) plus a stale test validating a rule no longer in production and three `switchBindFor` loader-chain tests silently lost when a legacy write path was deleted — all fixed and re-verified; verified via `npm run build`/`typecheck` (clean), `npm test` (159 files / 3262 tests), `npm run ui:verify` (68/68 screens, 0 axe violations) and `npm run ui:flow raw-save-cascades`/`external-edit-cascades`/`care-drift-sync-now` (all offline, green).
- 080 — Install R1Q2 from the community package · S18 · R1Q2 joins Q2PRO as a bootstrap-wizard engine choice, engine-scoped assembly so a demo/point-release archive can never satisfy a missing engine-role file (r1q2.exe, ref_r1gl.dll, baseq2/gamex86.dll; dedicated.exe excluded), a one-shot `vid_ref "r1gl"` config seed that never overwrites an existing config, an x86-runtime probe that fails actionably rather than claiming success, an engine-aware icon, and a checked-in GPLv3 license installed alongside external-source information in the wizard; a clean review found and fixed a real defect (a hardcoded developer-machine license path that would silently defeat AC8 on every other machine) plus two missing AC5 job-level tests and vague runtime-missing copy, all confirmed fixed by a second review pass; verified via `npm run build`/`typecheck` (clean), `npm test` (3287 tests, two unrelated pre-existing timing flakes each passing standalone), `npm run ui:verify` (34/34 screens, 0 axe violations) and `node scripts/flow.mjs bootstrap-r1q2`/`bootstrap-wizard` (both offline, green); GitHub publication of the community mirror and the real Windows OpenGL/map-load smoke test remain manual residue, out of this build's authority.
